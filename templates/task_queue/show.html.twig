{% extends 'base.html.twig' %}

{% block title %}TaskQueue{% endblock %}

{% block body %}
    <h1>TaskQueue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_queue.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task_queue.name }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ task_queue.comment }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ task_queue.priority }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_queue_index') }}">back to list</a>

    <a href="{{ path('app_task_queue_edit', {'id': task_queue.id}) }}">edit</a>

    {{ include('task_queue/_delete_form.html.twig') }}
{% endblock %}
