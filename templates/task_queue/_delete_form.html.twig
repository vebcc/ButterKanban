<form method="post" action="{{ path('app_task_queue_delete', {'id': task_queue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_queue.id) }}">
    <button class="btn">Delete</button>
</form>
