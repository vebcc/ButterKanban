<form method="post" action="{{ path('app_task_user_type_delete', {'id': task_user_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_user_type.id) }}">
    <button class="btn">Delete</button>
</form>
