{% extends 'base.html.twig' %}

{% block title %}TaskUserType{% endblock %}

{% block body %}
    <h1>TaskUserType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_user_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task_user_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_user_type_index') }}">back to list</a>

    <a href="{{ path('app_task_user_type_edit', {'id': task_user_type.id}) }}">edit</a>

    {{ include('task_user_type/_delete_form.html.twig') }}
{% endblock %}
