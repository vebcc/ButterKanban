{% extends 'base.html.twig' %}

{% block title %}Log{% endblock %}

{% block body %}
    <h1>Log</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ log.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ log.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_log_index') }}">back to list</a>

    <a href="{{ path('app_log_edit', {'id': log.id}) }}">edit</a>

    {{ include('log/_delete_form.html.twig') }}
{% endblock %}
